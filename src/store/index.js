import Vue from 'vue';
import Vuex from 'vuex';

import AddRecipe from './modules/add-recipe';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AddRecipe,
  },
});
